<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Resumen de Compra</title>
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
  </head>
  <body class="bg-gray-100 flex items-center justify-center min-h-screen">

    <div class="bg-white p-8 rounded-lg shadow-lg w-full max-w-4xl">
      <h1 class="text-4xl font-bold mb-6 text-center text-gray-800">Resumen de
        Compra</h1>

      <table class="min-w-full bg-white border border-gray-300 rounded-lg">
        <thead>
          <tr class="bg-gray-200 text-gray-600">
            <th class="py-3 px-4 border-b">Nombre</th>
            <th class="py-3 px-4 border-b">Precio</th>
            <th class="py-3 px-4 border-b">Cantidad</th>
            <th class="py-3 px-4 border-b">Total</th>
          </tr>
        </thead>
        <tbody class="text-gray-700">
          {{#each products}}
            <tr class="hover:bg-gray-100">
              <td class="py-3 px-4 border-b">{{title}}</td>
              <td class="py-3 px-4 border-b">${{price}}</td>
              <td class="py-3 px-4 border-b">{{quantity}}</td>
              <td class="py-3 px-4 border-b">${{multiply price quantity}}</td>
            </tr>
          {{/each}}
        </tbody>
      </table>

      <p class="mt-6 text-center text-lg text-gray-800">Gracias por tu compra.
        Tu código de ticket es:
        <span class="font-bold">{{ticketCode}}</span></p>
    </div>

    <script src="/path/to/handlebars.js"></script>
    <script src="/path/to/your-template.js"></script>
  </body>
</html>